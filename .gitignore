# SECRETS — retirer .env de cette liste = TruffleHog bloque
.env

# LINTER — retirer src/vuln.php = PHPCS bloque (code mal formaté + injection SQL)
src/vuln.php

# SECRETS — retirer src/config.php = TruffleHog bloque (clés AWS en dur)
src/config.php

# Dépendances
src/vendor/
